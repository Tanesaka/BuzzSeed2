      <% if user != current_user %>
      <% elsif user.beginner == false %>
        <p class="ml-2 mt-4">
          チュートリアル完了です！<br>
          引き続きBuzzSeedを盛り上げていきましょう。
        </p>
        <%= link_to "BuzzSeedの使い方をみる",about_path, class:"btn-info btn-sm mt-1 btn-block text-center"; %>
      <% else %>
        <h5 class="border-bottom border-gray mt-5 pt-2 font-weight-bold">
          <i class="fas fa-walking"></i> チュートリアル
        </h5>
        <div class="mt-3 ml-3">
          <%= link_to about_path(anchor: 'about2') do %>
            <p class="ml-1 mt-2">
              3つ以上のヒトコトにGood!<i class="fas fa-thumbs-up text-danger"></i>
          <% end %>
          <% if user.favorites.count < 3 %>
              <span class="ml-3 font-weight-bold">(<%= user.favorites.count %> / 3)</span>
          <% else %>
              <span class="ml-3 text-primary font-weight-bold">クリア！</span>
          <% end %>
            </p>
            
          <%= link_to about_path(anchor: 'about4') do %>
            <p class="ml-1 mt-2">
              お題を投稿
          <% end %>
          <% if user.themes.blank? %>
              <span class="ml-3 font-weight-bold">(0 / 1)</span>
          <% else %>
              <span class="ml-3 text-primary font-weight-bold">クリア！</span>
          <% end %>
            </p>

          <%= link_to about_path(anchor: 'about3') do %>
            <p class="ml-1 mt-2">
              2つ以上ヒトコトを投稿
          <% end %>
          <% if user.answers.count < 2 %>
             <span class="ml-3 font-weight-bold">(<%= user.answers.count %> / 2)</span>
          <% else %>
             <span class="ml-3 text-primary font-weight-bold">クリア！</span>
          <% end %>
            </p>

          <%= link_to about_path(anchor: 'about5') do %>
            <p class="ml-1 mt-2">
              3人以上のユーザーをフォロー
          <% end %>
          <% if user.followings.count < 3 %>
              <span class="ml-3 font-weight-bold">(<%= user.followings.count %> / 3)</span>
          <% else %>
              <span class="ml-3 text-primary font-weight-bold">クリア！</span>
          <% end %>
            </p>

          <%= link_to about_path(anchor: 'about7') do %>
            <p class="ml-1 mt-2">
              BuzzSeedBotを使う
          <% end %>
          <% if user.tweets.blank? %>
              <span class="ml-3 font-weight-bold">(0 / 1)</span>
          <% else %>
              <span class="ml-3 text-primary font-weight-bold">クリア！</span>
          <% end %>
            </p>
        </div>
        <%= link_to "BuzzSeedの使い方をみる",about_path, class:"btn-info btn-block btn-sm text-center mt-2 w-100"; %>
      <% end %>